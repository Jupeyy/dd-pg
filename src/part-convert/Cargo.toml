[package]
name = "part-convert"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.16", features = ["derive"] }
client-extra = { path = "../../game/client-extra" }
image = { path = "../../lib/image" }
