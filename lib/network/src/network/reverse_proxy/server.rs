/// The reverse proxy server reads the wrapped packets send by the
/// reverse proxy client.
/// Additionally it makes sure only reverse proxies connect to the server.
pub struct ReverseProxyServer {}
