[package]
name = "game-database-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
sql = { path = "../../lib/sql" }
game-database = { path = "../../lib/game-database" }

ddnet-account-sql = { version = "0.2.0", features = ["sqlite"] }

# https://github.com/launchbadge/sqlx/issues/2636
sqlx = { version = "=0.6.3", features = ["mysql", "sqlite", "any", "runtime-tokio-rustls", "chrono"] }
anyhow = { version = "1.0.89", features = ["backtrace"] }
async-trait = "0.1.83"
parking_lot = "0.12.3"


[dev-dependencies]
base = { path = "../../lib/base" }
base-io = { path = "../../lib/base-io" }

tokio = { version = "1.40.0", features = ["rt-multi-thread", "sync", "fs", "time", "macros"] }
